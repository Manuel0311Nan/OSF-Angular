import { PagesPhotoPipe } from './pages-photo.pipe';

describe('PagesPhotoPipe', () => {
  it('create an instance', () => {
    const pipe = new PagesPhotoPipe();
    expect(pipe).toBeTruthy();
  });
});
